name: Package Release

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - edited

jobs:
  validate-metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build drovr
        run: pnpm run build

      - name: Validate pull request title
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          node ./dist/cli.mjs package-release validate "${{ github.event.pull_request.title }}"

      - name: Validate pull request commits
        if: github.event_name == 'pull_request'
        run: |
          set -euo pipefail
          COMMITS_FILE=$(mktemp)
          git log --format=%B%x00 "origin/${{ github.base_ref }}..HEAD" > "$COMMITS_FILE"
          while IFS= read -r -d $'\0' commit; do
            [ -z "$commit" ] && continue
            node ./dist/cli.mjs package-release validate "$commit"
          done < "$COMMITS_FILE"
          rm -f "$COMMITS_FILE"

      - name: Validate pushed commits
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          if [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ] || [ -z "${{ github.event.before }}" ]; then
            RANGE="HEAD~1..HEAD"
          else
            RANGE="${{ github.event.before }}..${{ github.event.after }}"
          fi
          COMMITS_FILE=$(mktemp)
          git log --format=%B%x00 "$RANGE" > "$COMMITS_FILE"
          while IFS= read -r -d $'\0' commit; do
            [ -z "$commit" ] && continue
            node ./dist/cli.mjs package-release validate "$commit"
          done < "$COMMITS_FILE"
          rm -f "$COMMITS_FILE"

  release-please:
    runs-on: ubuntu-latest
    needs: validate-metadata
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      pull-requests: write
    outputs:
      releases_created: ${{ steps.release.outputs.releases_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      version: ${{ steps.release.outputs.version }}
      sha: ${{ github.sha }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build drovr
        run: pnpm run build

      - name: Run Release Please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      - name: Reconcile Release PR with canonical notes
        if: steps.release.outputs.pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_JSON: ${{ steps.release.outputs.pr }}
        run: |
          set -euo pipefail
          PR_NUMBER=$(node -e 'console.log(JSON.parse(process.env.PR_JSON).number)')
          PR_TITLE=$(node -e 'console.log(JSON.parse(process.env.PR_JSON).title || "")')
          PR_BODY=$(node -e 'console.log(JSON.parse(process.env.PR_JSON).body || "")')

          RESULT=$(node ./dist/cli.mjs package-release reconcile-pr --title "$PR_TITLE" --body "$PR_BODY" --json)
          ACTION=$(node -e 'console.log(JSON.parse(process.argv[1]).action)' "$RESULT")

          if [ "$ACTION" = "close" ]; then
            echo "No user-facing releasable changes in proposed range. Closing Release PR #$PR_NUMBER."
            gh pr close "$PR_NUMBER" --comment "Closed by Package Release automation: no user-facing changes (Breaking Changes, Added, Changed, Fixed, Removed) in proposed range."
          else
            UPDATED_BODY=$(node -e 'console.log(JSON.parse(process.argv[1]).body)' "$RESULT")
            gh pr edit "$PR_NUMBER" --body "$UPDATED_BODY"
          fi

      - name: Prepare draft GitHub Release
        if: steps.release.outputs.releases_created == 'true' || steps.release.outputs.tag_name
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ steps.release.outputs.tag_name }}
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          node ./dist/cli.mjs package-release prepare \
            --tag "$RELEASE_TAG" \
            --version "$RELEASE_VERSION" \
            --sha "$TARGET_SHA"

  publish-package:
    runs-on: ubuntu-latest
    needs: release-please
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && (needs.release-please.outputs.releases_created == 'true' || needs.release-please.outputs.tag_name != '')
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout repository at tag
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-please.outputs.tag_name }}
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint

      - name: Check formatting
        run: pnpm run fmt:check

      - name: Run tests
        run: pnpm test

      - name: Build drovr
        run: pnpm run build

      - name: Verify package tarball
        id: verify-pack
        run: |
          set -euo pipefail
          TARBALL_JSON=$(node ./dist/cli.mjs package-release verify-pack --keep-temp --json)
          TARBALL_PATH=$(node -e 'console.log(JSON.parse(process.argv[1]).tarballPath)' "$TARBALL_JSON")
          echo "tarball_path=$TARBALL_PATH" >> "$GITHUB_OUTPUT"

      - name: Publish to npm and finalize GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          RELEASE_TAG: ${{ needs.release-please.outputs.tag_name }}
          RELEASE_VERSION: ${{ needs.release-please.outputs.version }}
          TARGET_SHA: ${{ needs.release-please.outputs.sha }}
          TARBALL_PATH: ${{ steps.verify-pack.outputs.tarball_path }}
        run: |
          set -euo pipefail
          if [ -n "${NPM_TOKEN:-}" ]; then
            export NODE_AUTH_TOKEN="$NPM_TOKEN"
          fi
          node ./dist/cli.mjs package-release publish \
            --tag "$RELEASE_TAG" \
            --version "$RELEASE_VERSION" \
            --sha "$TARGET_SHA" \
            --tarball "$TARBALL_PATH"
